/**
 * External dependencies
 */
import React from 'react';

const GroupHeaderTitle = ({ children }) => (
    <div className="group__header-title">{children}</div>
);

export default GroupHeaderTitle;
