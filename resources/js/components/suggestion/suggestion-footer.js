/**
 * External dependencies
 */
import React from 'react';

const SuggestionFooter = ({ children }) => (
    <div className="suggestion__footer">{children}</div>
);

export default SuggestionFooter;
