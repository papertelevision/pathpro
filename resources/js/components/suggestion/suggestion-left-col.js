/**
 * External dependencies
 */
import React from 'react';

const SuggestionLeftCol = ({ children }) => (
    <div className="suggestion__left-col">{children}</div>
);

export default SuggestionLeftCol;
