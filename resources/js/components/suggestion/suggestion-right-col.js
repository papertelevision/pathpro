/**
 * External dependencies
 */
import React from 'react';

const SuggestionRightCol = ({ children }) => (
    <div className="suggestion__right-col">{children}</div>
);

export default SuggestionRightCol;
